diff --git a/providers/common/der/oids_to_c.pm b/providers/common/der/oids_to_c.pm
index a68dd9d..5735147 100644
--- a/providers/common/der/oids_to_c.pm
+++ b/providers/common/der/oids_to_c.pm
@@ -80,7 +80,6 @@ sub _process {
         # print STDERR $text;
         # print STDERR "-----END DEBUG-----\n";
-        use re 'debug';
         while ($text =~ m/${OID_def_re}/sg) {
             my $comment = $&;
             my $name = $1;
             my $value = $2;
diff --git a/util/mkinstallvars.pl b/util/mkinstallvars.pl
index 916f2f1..25eb1b3 100644
--- a/util/mkinstallvars.pl
+++ b/util/mkinstallvars.pl
@@ -35,7 +35,6 @@ my %all = ( );
 foreach (@absolutes) { $all{$_} = 1 }
 foreach (@subdirs) { foreach (@{$_->[1]}) { $all{$_} = 1 } }
 foreach (@others) { $all{$_} = 1 }
-print STDERR "DEBUG: all keys: ", join(", ", sort keys %all), "\n";
 
 my %keys = ();
 my %values = ();
@@ -45,6 +44,16 @@ foreach (@ARGV) {
     push @{$values{$k}}, $v;
 }
 
+# The build-tree invocation intentionally omits installation-only variables.
+# Treat those omissions as explicit empty values before diagnostics and path
+# normalization. This avoids generating undefined entries in installdata.pm,
+# which otherwise produce repeated Perl warnings during a normal macOS build.
+foreach my $k (keys %all) {
+    next if exists $values{$k};
+    $keys{$k} = 1;
+    $values{$k} = [ '' ];
+}
+
 # special case for LIBDIR vs libdir.
 # For installations, They both get their value from ./Configure's --libdir or
 # corresponding config target attribute, but LIBDIR only gets a value if the
@@ -52,9 +61,7 @@ foreach (@ARGV) {
 # the former doesn't have a value, we give it the latter's value, and rely
 # on mechanisms further down to do the rest of the processing.
 # If they're both empty, it's still fine.
-print STDERR "DEBUG: LIBDIR = $values{LIBDIR}->[0], libdir = $values{libdir}->[0] => ";
 $values{LIBDIR}->[0] = $values{libdir}->[0] unless $values{LIBDIR}->[0];
-print STDERR "LIBDIR = $values{LIBDIR}->[0]\n";
 
 # warn if there are missing values, and also if there are unexpected values
 foreach my $k (sort keys %all) {
@@ -69,10 +76,8 @@ foreach my $k (sort keys %keys) {
 foreach my $k (@absolutes) {
     my $v = $values{$k} || [ '.' ];
     die "Can't have more than one $k\n" if scalar @$v > 1;
-    print STDERR "DEBUG: $k = $v->[0] => ";
     $v = [ map { File::Spec->rel2abs($_) } @$v ];
     $values{$k} = $v;
-    print STDERR "$k = $v->[0]\n";
 }
 
 # Absolute paths for the subdir variables are computed.  This provides
@@ -88,9 +93,6 @@ foreach my $pair (@subdirs) {
         my $kr = "${k}_REL_${var}";
         my $v2 = $values{$k} || [ '.' ];
         $values{$k} = [];       # We're rebuilding it
-        print STDERR "DEBUG: $k = ",
-            (scalar @$v2 > 1 ? "[ " . join(", ", @$v2) . " ]" : $v2->[0]),
-            " => ";
         foreach my $v (@$v2) {
             if (File::Spec->file_name_is_absolute($v)) {
                 push @{$values{$k}}, $v;
@@ -102,14 +104,6 @@ foreach my $pair (@subdirs) {
                     File::Spec->rel2abs($v, $values{$var}->[0]);
             }
         }
-        print STDERR join(", ",
-                          map {
-                              my $v = $values{$_};
-                              "$_ = " . (scalar @$v > 1
-                                         ? "[ " . join(", ", @$v) . " ]"
-                                         : $v->[0]);
-                          } ($k, $kr)),
-            "\n";
     }
 }
 
