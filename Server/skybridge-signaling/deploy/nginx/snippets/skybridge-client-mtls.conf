# Include this inside the HTTPS server block only after app/device client
# certificates are provisioned. This mode rejects clients without a verified
# certificate and lets Node bind tokens to the verified certificate fingerprint.
ssl_client_certificate /etc/skybridge-signaling/client-ca.pem;
ssl_verify_client on;
ssl_verify_depth 2;
