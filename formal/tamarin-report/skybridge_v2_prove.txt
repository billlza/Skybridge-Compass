theory SkyBridge_V2 begin

// Function signature and definition of the equational theory E

functions: SuiteClassic/0, SuitePQCv1/0, SuitePQCv2/0, fst/1, h/1,
           pair/2, pk/1, sign/2, snd/1, true/0, verify/3
equations:
    fst(<x.1, x.2>) = x.1,
    snd(<x.1, x.2>) = x.2,
    verify(sign(x.1, x.2), x.1, pk(x.2)) = true









rule (modulo E) Init_Offer_Default:
   [ Fr( ~sid ) ]
  --[
  Policy( ~sid, 'default' ), OfferIncludes( ~sid, SuiteClassic ),
  OfferIncludes( ~sid, SuitePQCv1 ), OfferIncludes( ~sid, SuitePQCv2 ),
  OfferSent( ~sid, 'A' )
  ]->
   [ St_Default( ~sid ) ]

  /* has exactly the trivial AC variant */

rule (modulo E) Init_Offer_Strict:
   [ Fr( ~sid ) ]
  --[
  Policy( ~sid, 'strictPQC' ), OfferIncludes( ~sid, SuitePQCv1 ),
  OfferIncludes( ~sid, SuitePQCv2 ), OfferSent( ~sid, 'A' )
  ]->
   [ St_Strict( ~sid ) ]

  /* has exactly the trivial AC variant */

rule (modulo E) Default_Timeout_Denied:
   [ St_Default( sid ) ]
  --[ TimeoutObserved( sid ), FallbackDenied( sid, 'timeout' ) ]->
   [ St_Default_PQCOnly( sid ) ]

  /* has exactly the trivial AC variant */

rule (modulo E) Default_LocalFailure_Eligible:
   [ St_Default( sid ) ]
  --[
  LocalFallbackEligible( sid, 'local_capability_error' ),
  FallbackGatePassed( sid, 'local_capability_error' )
  ]->
   [ St_Default_FallbackCandidate( sid ) ]

  /* has exactly the trivial AC variant */

rule (modulo E) Default_Cooldown_Allows:
   [ St_Default_FallbackCandidate( sid ) ]
  --[ CooldownChecked( sid, 'inactive' ), CooldownPermit( sid, 'allow' )
  ]->
   [ St_Default_FallbackReady( sid ) ]

  /* has exactly the trivial AC variant */

rule (modulo E) Default_Cooldown_Denies:
   [ St_Default_FallbackCandidate( sid ) ]
  --[
  CooldownChecked( sid, 'active' ), CooldownDenied( sid, 'active_window' )
  ]->
   [ St_Default_PQCOnly( sid ) ]

  /* has exactly the trivial AC variant */

rule (modulo E) Neg_Default_Classic:
   [ St_Default_FallbackReady( sid ) ]
  --[
  SelectedSuite( sid, SuiteClassic ), SigBOverA( sid, SuiteClassic ),
  VerifiedSigB( sid, SuiteClassic ), Attempt( sid, SuiteClassic ),
  DowngradeEvent( sid ),
  DowngradeEventCtx( sid, 'local_capability_error', 'default',
                     'cooldown_checked'
  ),
  ClassicFallbackAccepted( sid )
  ]->
   [ St_HandshakeDone( sid, SuiteClassic ) ]

  /* has exactly the trivial AC variant */

rule (modulo E) Neg_Default_PQCv1:
   [ St_Default( sid ) ]
  --[
  SelectedSuite( sid, SuitePQCv1 ), SigBOverA( sid, SuitePQCv1 ),
  VerifiedSigB( sid, SuitePQCv1 ), Attempt( sid, SuitePQCv1 ),
  StaticSecretDerived( sid )
  ]->
   [ St_HandshakeDone( sid, SuitePQCv1 ) ]

  /* has exactly the trivial AC variant */

rule (modulo E) Neg_Default_PQCv2:
   [ St_Default( sid ) ]
  --[
  SelectedSuite( sid, SuitePQCv2 ), SigBOverA( sid, SuitePQCv2 ),
  VerifiedSigB( sid, SuitePQCv2 ), Attempt( sid, SuitePQCv2 ),
  StaticSecretDerived( sid ), EphemeralSecretDerived( sid ),
  HybridSecretComposed( sid )
  ]->
   [ St_HandshakeDone( sid, SuitePQCv2 ) ]

  /* has exactly the trivial AC variant */

rule (modulo E) Neg_Default_PQCv1_AfterFallbackBlock:
   [ St_Default_PQCOnly( sid ) ]
  --[
  SelectedSuite( sid, SuitePQCv1 ), SigBOverA( sid, SuitePQCv1 ),
  VerifiedSigB( sid, SuitePQCv1 ), Attempt( sid, SuitePQCv1 ),
  StaticSecretDerived( sid )
  ]->
   [ St_HandshakeDone( sid, SuitePQCv1 ) ]

  /* has exactly the trivial AC variant */

rule (modulo E) Neg_Default_PQCv2_AfterFallbackBlock:
   [ St_Default_PQCOnly( sid ) ]
  --[
  SelectedSuite( sid, SuitePQCv2 ), SigBOverA( sid, SuitePQCv2 ),
  VerifiedSigB( sid, SuitePQCv2 ), Attempt( sid, SuitePQCv2 ),
  StaticSecretDerived( sid ), EphemeralSecretDerived( sid ),
  HybridSecretComposed( sid )
  ]->
   [ St_HandshakeDone( sid, SuitePQCv2 ) ]

  /* has exactly the trivial AC variant */

rule (modulo E) Neg_Strict_PQCv1:
   [ St_Strict( sid ) ]
  --[
  SelectedSuite( sid, SuitePQCv1 ), SigBOverA( sid, SuitePQCv1 ),
  VerifiedSigB( sid, SuitePQCv1 ), Attempt( sid, SuitePQCv1 ),
  StaticSecretDerived( sid )
  ]->
   [ St_HandshakeDone( sid, SuitePQCv1 ) ]

  /* has exactly the trivial AC variant */

rule (modulo E) Neg_Strict_PQCv2:
   [ St_Strict( sid ) ]
  --[
  SelectedSuite( sid, SuitePQCv2 ), SigBOverA( sid, SuitePQCv2 ),
  VerifiedSigB( sid, SuitePQCv2 ), Attempt( sid, SuitePQCv2 ),
  StaticSecretDerived( sid ), EphemeralSecretDerived( sid ),
  HybridSecretComposed( sid )
  ]->
   [ St_HandshakeDone( sid, SuitePQCv2 ) ]

  /* has exactly the trivial AC variant */

rule (modulo E) Strict_Bootstrap_ControlOnly:
   [ St_Strict( sid ) ]
  --[
  BootstrapTrigger( sid, 'missingKEM' ), BootstrapControlChannel( sid ),
  BusinessTrafficBlocked( sid )
  ]->
   [ St_Strict_Bootstrap( sid ) ]

  /* has exactly the trivial AC variant */

rule (modulo E) Strict_Bootstrap_Rekey:
   [ St_Strict_Bootstrap( sid ) ]
  --[ PQCRekeyCompleted( sid ) ]->
   [ St_Strict_Rekeyed( sid ) ]

  /* has exactly the trivial AC variant */

rule (modulo E) Strict_Bootstrap_EnableBusiness:
   [ St_Strict_Rekeyed( sid ) ] --[ BusinessTrafficEnabled( sid ) ]-> [ ]

  /* has exactly the trivial AC variant */

rule (modulo E) Finished_Phase:
   [ St_HandshakeDone( sid, sel ) ]
  --[ FinishedSent( sid, 'B' ), FinishedVerified( sid, 'A' ) ]->
   [ St_Finished( sid, sel ) ]

  /* has exactly the trivial AC variant */

rule (modulo E) Session_Established:
   [ St_Finished( sid, sel ) ] --[ SessionEstablished( sid, sel ) ]-> [ ]

  /* has exactly the trivial AC variant */

lemma Exists_DefaultClassicTrace:
  exists-trace
  "∃ sid #i #j #k.
    ((Policy( sid, 'default' ) @ #i) ∧ (DowngradeEvent( sid ) @ #j)) ∧
    (SessionEstablished( sid, SuiteClassic ) @ #k)"
/*
guarded formula characterizing all satisfying traces:
"∃ sid #i #j #k.
  (Policy( sid, 'default' ) @ #i) ∧
  (DowngradeEvent( sid ) @ #j) ∧
  (SessionEstablished( sid, SuiteClassic ) @ #k)"
*/
simplify
solve( St_Default_FallbackReady( sid ) ▶₀ #j )
  case Default_Cooldown_Allows
  solve( St_Finished( ~sid, SuiteClassic ) ▶₀ #k )
    case Finished_Phase
    solve( Policy( ~sid, 'default' ) @ #i )
      case Init_Offer_Default
      SOLVED // trace found
    qed
  qed
qed

lemma Exists_StrictPQCv2Trace:
  exists-trace
  "∃ sid #i #j.
    (Policy( sid, 'strictPQC' ) @ #i) ∧
    (SessionEstablished( sid, SuitePQCv2 ) @ #j)"
/*
guarded formula characterizing all satisfying traces:
"∃ sid #i #j.
  (Policy( sid, 'strictPQC' ) @ #i) ∧
  (SessionEstablished( sid, SuitePQCv2 ) @ #j)"
*/
simplify
solve( St_Finished( sid, SuitePQCv2 ) ▶₀ #j )
  case Finished_Phase_case_4
  solve( Policy( ~sid, 'strictPQC' ) @ #i )
    case Init_Offer_Strict
    SOLVED // trace found
  qed
qed

lemma Exists_StrictBootstrapRecoveryTrace:
  exists-trace
  "∃ sid #i #j #k.
    ((Policy( sid, 'strictPQC' ) @ #i) ∧
     (BootstrapControlChannel( sid ) @ #j)) ∧
    (PQCRekeyCompleted( sid ) @ #k)"
/*
guarded formula characterizing all satisfying traces:
"∃ sid #i #j #k.
  (Policy( sid, 'strictPQC' ) @ #i) ∧
  (BootstrapControlChannel( sid ) @ #j) ∧
  (PQCRekeyCompleted( sid ) @ #k)"
*/
simplify
solve( St_Strict( sid ) ▶₀ #j )
  case Init_Offer_Strict
  solve( St_Strict_Bootstrap( ~sid ) ▶₀ #k )
    case Strict_Bootstrap_ControlOnly
    solve( Policy( ~sid, 'strictPQC' ) @ #i )
      case Init_Offer_Strict
      SOLVED // trace found
    qed
  qed
qed

lemma NoClassicUnderStrictPQC:
  all-traces
  "∀ sid #i.
    (Policy( sid, 'strictPQC' ) @ #i) ⇒
    (¬(∃ #j. SessionEstablished( sid, SuiteClassic ) @ #j))"
/*
guarded formula characterizing all counter-examples:
"∃ sid #i.
  (Policy( sid, 'strictPQC' ) @ #i)
 ∧
  ∃ #j. (SessionEstablished( sid, SuiteClassic ) @ #j)"
*/
simplify
solve( St_Finished( sid, SuiteClassic ) ▶₀ #j )
  case Finished_Phase
  by solve( Policy( ~sid, 'strictPQC' ) @ #i )
qed

lemma NegotiationIntegrity_SelectedWasOffered:
  all-traces
  "∀ sid sel #i.
    (SelectedSuite( sid, sel ) @ #i) ⇒
    (∃ #j. OfferIncludes( sid, sel ) @ #j)"
/*
guarded formula characterizing all counter-examples:
"∃ sid sel #i.
  (SelectedSuite( sid, sel ) @ #i)
 ∧
  ∀ #j. (OfferIncludes( sid, sel ) @ #j) ⇒ ⊥"
*/
simplify
solve( SelectedSuite( sid, sel ) @ #i )
  case Neg_Default_Classic
  solve( St_Default_FallbackReady( sid ) ▶₀ #i )
    case Default_Cooldown_Allows
    by contradiction /* from formulas */
  qed
next
  case Neg_Default_PQCv1
  solve( St_Default( sid ) ▶₀ #i )
    case Init_Offer_Default
    by contradiction /* from formulas */
  qed
next
  case Neg_Default_PQCv1_AfterFallbackBlock
  solve( St_Default_PQCOnly( sid ) ▶₀ #i )
    case Default_Cooldown_Denies
    by contradiction /* from formulas */
  next
    case Default_Timeout_Denied
    by contradiction /* from formulas */
  qed
next
  case Neg_Default_PQCv2
  solve( St_Default( sid ) ▶₀ #i )
    case Init_Offer_Default
    by contradiction /* from formulas */
  qed
next
  case Neg_Default_PQCv2_AfterFallbackBlock
  solve( St_Default_PQCOnly( sid ) ▶₀ #i )
    case Default_Cooldown_Denies
    by contradiction /* from formulas */
  next
    case Default_Timeout_Denied
    by contradiction /* from formulas */
  qed
next
  case Neg_Strict_PQCv1
  solve( St_Strict( sid ) ▶₀ #i )
    case Init_Offer_Strict
    by contradiction /* from formulas */
  qed
next
  case Neg_Strict_PQCv2
  solve( St_Strict( sid ) ▶₀ #i )
    case Init_Offer_Strict
    by contradiction /* from formulas */
  qed
qed

lemma AuthenticationRequiresVerifiedSigB:
  all-traces
  "∀ sid sel #i.
    (SessionEstablished( sid, sel ) @ #i) ⇒
    (∃ #j. VerifiedSigB( sid, sel ) @ #j)"
/*
guarded formula characterizing all counter-examples:
"∃ sid sel #i.
  (SessionEstablished( sid, sel ) @ #i)
 ∧
  ∀ #j. (VerifiedSigB( sid, sel ) @ #j) ⇒ ⊥"
*/
simplify
solve( St_Finished( sid, sel ) ▶₀ #i )
  case Finished_Phase_case_1
  by contradiction /* from formulas */
next
  case Finished_Phase_case_2
  by contradiction /* from formulas */
next
  case Finished_Phase_case_3
  by contradiction /* from formulas */
next
  case Finished_Phase_case_4
  by contradiction /* from formulas */
next
  case Finished_Phase_case_5
  by contradiction /* from formulas */
next
  case Finished_Phase_case_6
  by contradiction /* from formulas */
next
  case Finished_Phase_case_7
  by contradiction /* from formulas */
next
  case Finished_Phase_case_8
  by contradiction /* from formulas */
next
  case Finished_Phase_case_9
  by contradiction /* from formulas */
qed

lemma ClassicRequiresDowngradeEvent:
  all-traces
  "∀ sid #i.
    (SessionEstablished( sid, SuiteClassic ) @ #i) ⇒
    (∃ #j. DowngradeEvent( sid ) @ #j)"
/*
guarded formula characterizing all counter-examples:
"∃ sid #i.
  (SessionEstablished( sid, SuiteClassic ) @ #i)
 ∧
  ∀ #j. (DowngradeEvent( sid ) @ #j) ⇒ ⊥"
*/
simplify
solve( St_Finished( sid, SuiteClassic ) ▶₀ #i )
  case Finished_Phase
  by contradiction /* from formulas */
qed

lemma ClassicRequiresFallbackGate:
  all-traces
  "∀ sid #i.
    (SessionEstablished( sid, SuiteClassic ) @ #i) ⇒
    (∃ #j. FallbackGatePassed( sid, 'local_capability_error' ) @ #j)"
/*
guarded formula characterizing all counter-examples:
"∃ sid #i.
  (SessionEstablished( sid, SuiteClassic ) @ #i)
 ∧
  ∀ #j. (FallbackGatePassed( sid, 'local_capability_error' ) @ #j) ⇒ ⊥"
*/
simplify
solve( St_Finished( sid, SuiteClassic ) ▶₀ #i )
  case Finished_Phase
  by contradiction /* from formulas */
qed

lemma ClassicOnlyUnderDefaultPolicy:
  all-traces
  "∀ sid #i.
    (SessionEstablished( sid, SuiteClassic ) @ #i) ⇒
    (∃ #j. Policy( sid, 'default' ) @ #j)"
/*
guarded formula characterizing all counter-examples:
"∃ sid #i.
  (SessionEstablished( sid, SuiteClassic ) @ #i)
 ∧
  ∀ #j. (Policy( sid, 'default' ) @ #j) ⇒ ⊥"
*/
simplify
solve( St_Finished( sid, SuiteClassic ) ▶₀ #i )
  case Finished_Phase
  by contradiction /* from formulas */
qed

lemma V2HybridSecretComposition:
  all-traces
  "∀ sid #i.
    (SessionEstablished( sid, SuitePQCv2 ) @ #i) ⇒
    (∃ #j #k.
      (StaticSecretDerived( sid ) @ #j) ∧
      (EphemeralSecretDerived( sid ) @ #k))"
/*
guarded formula characterizing all counter-examples:
"∃ sid #i.
  (SessionEstablished( sid, SuitePQCv2 ) @ #i)
 ∧
  ∀ #j #k.
   (StaticSecretDerived( sid ) @ #j) ∧ (EphemeralSecretDerived( sid ) @ #k)
  ⇒
   ⊥"
*/
simplify
solve( St_Finished( sid, SuitePQCv2 ) ▶₀ #i )
  case Finished_Phase_case_1
  by contradiction /* from formulas */
next
  case Finished_Phase_case_2
  by contradiction /* from formulas */
next
  case Finished_Phase_case_3
  by contradiction /* from formulas */
next
  case Finished_Phase_case_4
  by contradiction /* from formulas */
qed

lemma TimeoutCannotTriggerClassicFallback:
  all-traces
  "∀ sid #i.
    (TimeoutObserved( sid ) @ #i) ⇒
    (¬(∃ #j. SessionEstablished( sid, SuiteClassic ) @ #j))"
/*
guarded formula characterizing all counter-examples:
"∃ sid #i.
  (TimeoutObserved( sid ) @ #i)
 ∧
  ∃ #j. (SessionEstablished( sid, SuiteClassic ) @ #j)"
*/
simplify
solve( St_Default( sid ) ▶₀ #i )
  case Init_Offer_Default
  by solve( St_Finished( ~sid, SuiteClassic ) ▶₀ #j )
qed

lemma FallbackRateLimitEnforced:
  all-traces
  "∀ sid #i.
    (SessionEstablished( sid, SuiteClassic ) @ #i) ⇒
    (∃ #j #k.
      (CooldownChecked( sid, 'inactive' ) @ #j) ∧
      (CooldownPermit( sid, 'allow' ) @ #k))"
/*
guarded formula characterizing all counter-examples:
"∃ sid #i.
  (SessionEstablished( sid, SuiteClassic ) @ #i)
 ∧
  ∀ #j #k.
   (CooldownChecked( sid, 'inactive' ) @ #j) ∧
   (CooldownPermit( sid, 'allow' ) @ #k)
  ⇒
   ⊥"
*/
simplify
solve( St_Finished( sid, SuiteClassic ) ▶₀ #i )
  case Finished_Phase
  by contradiction /* from formulas */
qed

lemma DowngradeEventRequiredWithContext:
  all-traces
  "∀ sid #i.
    (SessionEstablished( sid, SuiteClassic ) @ #i) ⇒
    (∃ #j.
      DowngradeEventCtx( sid, 'local_capability_error', 'default',
                         'cooldown_checked'
      ) @ #j)"
/*
guarded formula characterizing all counter-examples:
"∃ sid #i.
  (SessionEstablished( sid, SuiteClassic ) @ #i)
 ∧
  ∀ #j.
   (DowngradeEventCtx( sid, 'local_capability_error', 'default',
                       'cooldown_checked'
    ) @ #j)
  ⇒
   ⊥"
*/
simplify
solve( St_Finished( sid, SuiteClassic ) ▶₀ #i )
  case Finished_Phase
  by contradiction /* from formulas */
qed

lemma BootstrapControlOnlyUntilPQCRekey:
  all-traces
  "∀ sid #i.
    (BusinessTrafficEnabled( sid ) @ #i) ⇒
    (∃ #j. (PQCRekeyCompleted( sid ) @ #j) ∧ (#j < #i))"
/*
guarded formula characterizing all counter-examples:
"∃ sid #i.
  (BusinessTrafficEnabled( sid ) @ #i)
 ∧
  ∀ #j. (PQCRekeyCompleted( sid ) @ #j) ⇒ ¬(#j < #i)"
*/
simplify
solve( St_Strict_Rekeyed( sid ) ▶₀ #i )
  case Strict_Bootstrap_Rekey
  by contradiction /* from formulas */
qed























/* All wellformedness checks were successful. */

/*
Generated from:
Tamarin version 1.10.0
Maude version 2.7.1
Git revision: UNKNOWN, branch: UNKNOWN
Compiled at: 2024-10-30 13:42:18.081591 UTC
*/

end

==============================================================================
summary of summaries:

analyzed: skybridge_v2.spthy

  processing time: 0.19s
  
  Exists_DefaultClassicTrace (exists-trace): verified (5 steps)
  Exists_StrictPQCv2Trace (exists-trace): verified (4 steps)
  Exists_StrictBootstrapRecoveryTrace (exists-trace): verified (5 steps)
  NoClassicUnderStrictPQC (all-traces): verified (3 steps)
  NegotiationIntegrity_SelectedWasOffered (all-traces): verified (18 steps)
  AuthenticationRequiresVerifiedSigB (all-traces): verified (11 steps)
  ClassicRequiresDowngradeEvent (all-traces): verified (3 steps)
  ClassicRequiresFallbackGate (all-traces): verified (3 steps)
  ClassicOnlyUnderDefaultPolicy (all-traces): verified (3 steps)
  V2HybridSecretComposition (all-traces): verified (6 steps)
  TimeoutCannotTriggerClassicFallback (all-traces): verified (3 steps)
  FallbackRateLimitEnforced (all-traces): verified (3 steps)
  DowngradeEventRequiredWithContext (all-traces): verified (3 steps)
  BootstrapControlOnlyUntilPQCRekey (all-traces): verified (3 steps)

==============================================================================
