# 📋 部署前检查清单

在部署到生产环境之前，请确认以下各项：

## 🔐 安全配置

- [ ] 已将 `env.example` 复制为 `.env` 并填写所有必需的配置
- [ ] 所有密钥和密码都已更改为强密码（不使用示例中的默认值）
- [ ] `SUPABASE_SERVICE_ROLE_KEY` 仅在后端使用，未暴露到前端
- [ ] `JWT_SECRET` 与 Supabase 项目设置中的值一致
- [ ] `.env` 文件已添加到 `.gitignore`，不会被提交到 Git
- [ ] 生产环境的 `CORS_ORIGINS` 已设置为具体域名（不使用 `*`）
- [ ] `.env` 文件权限设置为 600（`chmod 600 .env`）

## 🌐 网络和域名

- [ ] 域名已正确配置并指向服务器 IP
- [ ] SSL 证书已配置（使用 Let's Encrypt 或其他证书颁发机构）
- [ ] 防火墙规则已配置，允许 80 (HTTP) 和 443 (HTTPS) 端口
- [ ] DNS 记录已生效（可使用 `dig` 或 `nslookup` 验证）

## 🗄️ 数据库

- [ ] Supabase 项目已创建并配置
- [ ] 数据库连接字符串正确无误
- [ ] 数据库迁移已执行（如有）
- [ ] 数据库备份策略已设置
- [ ] 数据库访问权限已正确配置

## 🐳 Docker 配置

- [ ] Docker 和 Docker Compose 已安装（版本符合要求）
- [ ] `.dockerignore` 文件已配置，排除不必要的文件
- [ ] Docker 镜像可以成功构建（`docker-compose build`）
- [ ] 容器可以正常启动（`docker-compose up -d`）
- [ ] 健康检查通过（`docker-compose ps` 显示 healthy）

## 📦 前端

- [ ] 环境变量已正确设置（`VITE_SUPABASE_URL` 等）
- [ ] 生产构建成功（`pnpm build:prod`）
- [ ] 静态资源已优化（图片、字体等）
- [ ] 浏览器控制台无错误
- [ ] 所有页面可以正常访问
- [ ] 响应式设计在移动设备上表现正常

## 🔧 后端

- [ ] 后端环境变量已正确设置
- [ ] API 端点可以正常访问
- [ ] 认证和授权功能正常
- [ ] API 响应时间符合预期
- [ ] 错误处理机制完善
- [ ] 日志记录正常工作

## 🎨 UI/UX

- [ ] 星云背景效果正常显示
- [ ] 导航栏在所有页面正常工作
- [ ] 表单验证功能正常
- [ ] 加载动画和过渡效果流畅
- [ ] 无样式错乱或布局问题
- [ ] 跨浏览器兼容性良好（Chrome、Firefox、Safari、Edge）

## ⚡ 性能

- [ ] Lighthouse 评分 > 90（性能、无障碍、最佳实践、SEO）
- [ ] 首次内容绘制 (FCP) < 1.8s
- [ ] 最大内容绘制 (LCP) < 2.5s
- [ ] 首次输入延迟 (FID) < 100ms
- [ ] 累积布局偏移 (CLS) < 0.1
- [ ] 静态资源已启用缓存
- [ ] Gzip/Brotli 压缩已启用

## 📊 监控和日志

- [ ] 日志级别设置正确（生产环境通常为 `info` 或 `warn`）
- [ ] 日志轮转已配置（避免日志文件过大）
- [ ] 错误追踪工具已集成（如 Sentry）
- [ ] 性能监控工具已配置（如 Google Analytics）
- [ ] 健康检查端点可访问
- [ ] 告警机制已设置（服务宕机时通知）

## 🔄 备份和恢复

- [ ] 数据库自动备份已启用
- [ ] 备份恢复流程已测试
- [ ] 重要配置文件已备份
- [ ] 灾难恢复计划已制定

## 📱 移动端

- [ ] 移动端布局正常
- [ ] 触摸操作响应灵敏
- [ ] 移动端导航菜单正常工作
- [ ] 表单在移动设备上易于使用
- [ ] 页面加载速度符合预期

## 🧪 测试

- [ ] 所有主要功能已测试
- [ ] 用户注册和登录流程正常
- [ ] 账户绑定和解绑功能正常
- [ ] 错误场景已测试（网络错误、API 错误等）
- [ ] 边界情况已考虑（空数据、超长输入等）

## 📄 文档

- [ ] README.md 已更新
- [ ] API 文档已编写（如有必要）
- [ ] 部署文档已完善
- [ ] 用户使用指南已准备（如有必要）

## 🚀 部署流程

- [ ] 部署脚本已测试（`./deploy.sh`）
- [ ] 回滚计划已制定
- [ ] 部署通知机制已设置
- [ ] 部署时间窗口已确定（低峰期）

## ✅ 最终检查

- [ ] 所有以上项目都已确认
- [ ] 团队成员已知晓部署计划
- [ ] 紧急联系方式已准备
- [ ] 准备好开始部署！

---

## 🎯 快速检查命令

```bash
# 检查 Docker 状态
docker --version
docker-compose --version

# 检查环境变量
cat .env

# 测试构建
docker-compose build

# 启动服务
docker-compose up -d

# 检查服务状态
docker-compose ps

# 查看日志
docker-compose logs -f

# 健康检查
curl http://localhost/health
curl http://localhost:3000/health

# 检查端口占用
lsof -i :80
lsof -i :3000
```

## 💡 部署建议

1. **首次部署时，先在测试环境验证**
2. **准备好回滚方案**
3. **在低峰期进行部署**
4. **部署后持续监控 15-30 分钟**
5. **保持团队沟通畅通**

准备就绪后，执行：

```bash
./deploy.sh
```

祝部署顺利！🎉

